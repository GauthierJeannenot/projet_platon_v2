{% extends 'layout.html.twig' %}

{# {% block title %}Création fiche utilisateur -
	{{ parent() }}
{% endblock title %} #}

{% block stylesheets %}
<link rel="stylesheet" href="{{asset('assets/styles/espace_perso.css')}}">

{% endblock %}

{% block innerContent %}
 
	 {{form_start(form)}}
	<hr>
	{{form_row(form.classType)}}
	<hr>
	{{form_row(form.bio)}}
	<hr>
	{{form_row(form.opinion)}}
	<hr>
	{{form_row(form.personnality)}}
	<hr>
    {{form_row(form.subCategories)}}
	{{form_end(form)}}

 



{% endblock innerContent %}


