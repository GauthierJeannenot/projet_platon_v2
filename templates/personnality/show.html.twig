{% extends 'base.html.twig' %}

{% block title %}Personnality{% endblock %}

{% block body %}
    <h1>Personnality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnality.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ personnality.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personnality_index') }}">back to list</a>

    <a href="{{ path('app_personnality_edit', {'id': personnality.id}) }}">edit</a>

    {{ include('personnality/_delete_form.html.twig') }}
{% endblock %}
